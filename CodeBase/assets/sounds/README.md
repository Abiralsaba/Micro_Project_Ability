# Assets — Sounds

Notification tones, alert sounds, and TTS fallback audio.

## Expected Files

- `notification.wav` — New message received
- `alert.wav` — Health alert triggered
- `connected.wav` — Module connected to hub
- `error.wav` — Connection error
