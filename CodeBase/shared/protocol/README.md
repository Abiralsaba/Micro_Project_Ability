# Shared — AMP Protocol

AMP (Ability Message Protocol) — JSON schema, serialization, and validation utilities.

## Files

| File | Purpose |
|---|---|
| `amp_schema.json` | JSON Schema definition (already created) |
| `amp.py` | Python class for creating, validating, and serializing AMP messages |
