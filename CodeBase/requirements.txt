# Project Ability — Python Dependencies
# Install: pip install -r requirements.txt

# ── MediaPipe (for Glove Module + Gaze Module) ───────────────
mediapipe>=0.10.0

# ── Machine Learning ─────────────────────────────────────────
tensorflow>=2.15.0
# tflite-runtime>=2.14.0       # Use this instead of full TF on RPi Zero
numpy>=1.24.0
scikit-learn>=1.3.0

# ── Speech ───────────────────────────────────────────────────
vosk>=0.3.45                    # Offline STT
piper-tts>=1.2.0                # Offline TTS

# ── MQTT ─────────────────────────────────────────────────────
paho-mqtt>=2.0.0

# ── Encryption ───────────────────────────────────────────────
pycryptodome>=3.20.0            # AES-256-GCM

# ── Utilities ────────────────────────────────────────────────
pyyaml>=6.0                     # Configuration parsing
scipy>=1.11.0                   # Gaussian smoothing (gaze module)
Pillow>=10.0.0                  # Image processing
pyserial>=3.5                   # Serial communication (ESP32 ↔ RPi Zero)

# ── Testing ──────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0

# ── UI (Central Hub only) ────────────────────────────────────
# flask>=3.0.0                  # Web-based admin UI (option A)
# PyQt5>=5.15.0                 # Native Qt UI (option B)
